import styled from "styled-components";

export const ContainerMembersArea = styled.div`
`

export const ContainerModules = styled.div`
    margin: 0 auto;

    width: 70rem;
    height: 19.875rem;
    max-width: 67.5rem; 

    color: #fff;
    overflow: hidden;
    background: #141414;
`

export const ListModules = styled.div`
    gap: 1rem;

    display: flex;
    align-items: center;
    justify-content: center;

    transition: transform 0.3s ease;
`